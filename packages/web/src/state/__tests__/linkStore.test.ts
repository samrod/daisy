// No imports needed; all usage is via require()
describe('linkStore', () => {
  it('should be defined', () => {
    const linkStore = require('../linkStore');
    expect(linkStore).toBeDefined();
  });
});
