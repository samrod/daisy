// No imports needed; all usage is via require()
describe('guideStore', () => {
  it('should be defined', () => {
    const guideStore = require('../guideStore');
    expect(guideStore).toBeDefined();
  });
});
